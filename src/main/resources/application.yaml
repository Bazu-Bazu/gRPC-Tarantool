server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: gRPC-Tarantool-System

tarantool:
  host: ${TARANTOOL_SERVER_HOST:localhost}
  port: ${TARANTOOL_SERVER_PORT:3301}
  user: user
  password: password

grpc:
  server:
    port: ${GRPC_SERVER_PORT:9090}